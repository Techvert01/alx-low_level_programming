#include <stdio.h>
/**
 * main - C that prints various types
 * Return: 0
 */
int main(void)
{
	char a;
	int b;
	long int c;
	long long int d;
	float e;

	printf("Size of a char: %lu byte(S)\n", (unsigneed long)size of(a));
	printf("Size of an int: %lu byte(S)\n", (unsigneed long)size of(b));
	printf("Size of a long int: %lu byte(S)\n", (unsigneed long)size of(c));
	printf("Size of a long long int: %lu byte(S)\n", (unsigneed long)size of(d));
	printf("Size of a float: %lu byte(S)\n", (unsigneed long)size of(e));
	return (0)
}
